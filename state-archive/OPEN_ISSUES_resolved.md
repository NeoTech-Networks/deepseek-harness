## 2026-09-24 - closed by the 0.1.7-rc.1 install (archived from OPEN_ISSUES.md)

35. [CLOSED 2026-09-24, INSTALLED AND VERIFIED] Route (B) was taken and finished: 0.1.7-rc.1 was packaged in `C:\d17` (after the Visual Studio 2022 Build Tools install) and installed by the operator with `finish-install.ps1`: `SETUP COMPLETE. Running 0.1.7-rc.1`, exit 0, one uninstall row (`7808434f-469e-5eba-848e-edf64d3b94ce`, `DisplayVersion 0.1.7-rc.1`), 5 processes, 34 of 34 features read from the installed `app.asar`, migration 7 of 7 sections imported, AGENTS.md hardlink OK, 7 of 7 MCP servers and 14 of 14 hook calls verified live. See CURRENT_STATE and VERIFICATION_RESULTS 2026-09-24 and `C:\Projects\logs\2026-09-24\dsh-017-port\`. The 0.1.6 branch `update/v0.1.6-alpha.2` is no longer needed. Original wording preserved verbatim:

35. [PORTED AND GATED 2026-09-24; PACKAGING BLOCKED ON MSVC BUILD TOOLS] The chosen route was (B), port onto 0.1.7's architecture, retargeted at dsh-v0.1.7-rc.1. Branch `update/v0.1.7-rc.1` (`0c78ccfcef`) carries every feature, the settings migration and the rebuilt installer script; all gates pass (CURRENT_STATE 2026-09-24). Remaining: install Visual Studio 2022 Build Tools with the C++ workload and a Windows SDK (elevated, operator), package, prove markers in the packaged app.asar, hand the install to the operator. ORIGINAL ENTRY FOLLOWS. [OPEN, BLOCKER, found 2026-09-20] THE 0.1.6 UPDATE CANNOT BE TAKEN BY REBASING, BECAUSE UPSTREAM DELETED TWO LAYERS THE FORK IS BUILT ON. `dsh-v0.1.6-alpha.2` (`ddefc45fbc`) removes the desktop provisioning layer AND about a dozen packages the fork's own features live in, confirmed by direct reads on the tag, not from notes. LAYER 1, DELETED: `apps/desktop/src/seed-store.ts`, `src/provision-log.ts`, `scripts/prepare-seed.ts`, `scripts/macos-seed-store.ts`, `src/preload.ts`, the three `renderer/plugin-manager.*` files, `PACKAGING.md` and three specs are all ABSENT at 0.1.6 (12 files); all four provision milestones (`seed integrity verified`, `staged health check passed`, `staging profile activated as`, `applyRelease finished`) return ZERO files there, and `verifySeedIntegrity` and `integrity.json` are NOT FOUND. Upstream replaced the model with a runtime-tree / update-journal / mandatory-update architecture. `finish-install.ps1` references `seed` 21 times, `provision-*.log` 3, `integrity.json` 2 and `pending.json` 2, so it cannot drive 0.1.6 as written, and the plan's Phase 4/5 verification design (which read the provision log by CONTENT) has no target on that tag. LAYER 2, DELETED: `packages/session-status/*` (all three), `client/ui-sessions-panel`, `client/ui-sidebar-explorer`, `vision/routing`, `llm/account-usage`, `llm/llm-route-fallback`, `api/pinned-files`, `fs/tool-present`, `code-runtime/*`, `e2b/*`, `workflow/workflow-worker-thread`, `experimental/code-runtime-python`; `derivePhase` does not exist at 0.1.6. These are where the fork's features live, so `session-status-failed`, `session-stage-marks`, `account-usage`, `pinned-files` and the All Sessions section have no home there. A REBASE CANNOT RECOVER EITHER: the local stack NEVER modified `seed-store.ts` (`git log dsh-v0.1.5-rc.2..42db6c8038 -- apps/desktop/src/seed-store.ts` is empty), so replaying local diffs can never restore an upstream deletion. Measured before stopping: 8 of 53 local commits replayed. THE GATES CANNOT CATCH A WRONG CHOICE HERE, which is why it was escalated rather than decided: either route can typecheck, build and package while leaving first-run provisioning broken. SCALE: 6 of the 53 local commits touch `apps/desktop/`, 49 do not. OPTIONS RECORDED for whenever this is taken up: (A) keep the fork's desktop layer, restore the 12 files and adapt upstream's callers, forgoing 0.1.6's entire desktop rework; (B) port the fork's provisioning behaviour onto the new architecture, which is real engineering and needs its own plan. OPERATOR DECISION 2026-09-20: install the proven 0.1.5-rc.2 build now (the plan's bounded-effort fallback) and treat 0.1.6 as its own project. PRESERVED: branch `update/v0.1.6-alpha.2` in `C:\Projects\worktrees\dsh-update-v0.1.6-alpha.2` with its 8 commits, `C:\Projects\temp\dsh-resolve.py`, `C:\Projects\temp\dsh-replay-shas.txt` and `C:\Projects\temp\dsh-replay-progress.txt`.

## 2026-09-20 - closed by the footer live-intent save (archived from OPEN_ISSUES.md; the footer is live in the installed build)

33. [BUILT, PACKAGED, REFS PUSHED 2026-09-19; INSTALL PENDING, AND IT CLEARS ITEM 28's LAST VISUAL GAP] THE FOOTER NOW FOLLOWS THE DASHBOARD A SESSION NAMES, LIVE, AND THE NEWEST MENTION WINS. Operator report that started it: "the footer where it shows the dashboard and the design project is hidden when I do anything in the vercel directory ... fix that so it displays the dashboard URL since I'm always publishing to a dash". TWO DEFECTS, both measured. (1) THE FOOTER COULD NOT MOVE WHILE A SESSION RAN: it rendered the Session list row, and a row's links were computed once, at a list pull (the client pulls the list on boot and on reconnect only), so a Session started in the app kept an empty row forever and a past Session kept whatever its folder alone said. (2) THE RULE READ THE OLDEST OPERATOR MESSAGE AND REQUIRED A KNOWN KEY IN THE ADDRESS, but 17 of the 66 mapped page-source URLs route somewhere whose last segment is NOT their key (`gbl-content-dashboard` is served at `/gbl-overlord`, every `content-roadmap-*` key lives under `/content-roadmap/<client>`, `backlinks-profile` at `/backlinks`), so pasting the estate's own address resolved nothing. THE FIX: a client-visible Session projection `workspaceLinks`, whose fold is MAP-FREE (the addresses and `/dashboard` targets each operator message carries, at most 8 candidate-bearing messages retained, a message with no candidate returns the same state reference) and whose wire view resolves against the live maps at read time, so a regenerated map needs no restart. Candidates are read NEWEST FIRST; an address resolves EXACTLY as the maps record it first (`byAddress`, keyed `host + path`) which is what recovers the routes above, then by host-plus-key; `railway.neotech.biz` and `railway.theseoitguy.net` are accepted as hosts, still requiring a known key. The footer reads it through `useProjection` and keeps the list row as its fallback, and the host's existing `projection` frame plus the follow-opening baseline is what makes it update while the operator works, with no list pull and no restart. The list path reads the ALREADY-MATERIALIZED value only (`cachedSnapshot`), so it still never folds history. MEASURED over the 321 REAL Vercel Session logs, not fixtures: old rule 51, new rule 52, LOST 0, workspace-directory-alone 0; evidence `C:\Projects\logs\2026-09-19-dsh-footer-live-intent\` (`EVIDENCE.md`, `intent-proof.ts/.txt`, `intent-gap.ts/.txt`). THE REMAINING GAP IS EXPLICIT: 268 Sessions still resolve nothing, 45 of them ordinary operator Sessions; 14 of those name a dashboard only as a BARE WORD (`cfo`, `morgan`, `core30`, ...) and were deliberately NOT matched, because several dashboard keys are also ordinary words or a colleague's name, and the safe variant (a backticked or quoted key) would gain exactly ONE root Session. GATES: both touched suites 1195 passed / 1 failed (the known environmental `media-references` symlink `EPERM`), typecheck exit 0, build exit 0 with 240 client artifacts, oxlint 0 on the four changed directories, and the pre-existing `verify-export-jsdoc`, `verify-client-ui-i18n` and `test:docs` findings reproduced byte-identically on the untouched release worktree. Commit `d9e83fca02` on `fix/session-footer-live-intent`, fast-forward merged into `update/v0.1.5-rc.2`, both refs pushed and read back at `d9e83fca02`. Installer `deepseek-harness-0.1.5-rc.2-win-x64.exe`, 194,952,772 bytes, 2026-09-19 18:24:52, with `workspaceLinks`, `resolveSessionLinks` and `byAddress` proven inside the packaged seed archives. Registry 25 rows (`session-footer-live-intent`, `session-footer-live-read`). WHAT IS LEFT: the operator runs `finish-install.ps1` from `C:\Projects\worktrees\dsh-footer-live`, then the three markers are read out of the RUNNING profile and the populated footer is PHOTOGRAPHED, the one thing owed since 2026-09-13 (items 28 and 32).
32. [INSTALLED AND VERIFIED AT THE CODE LEVEL 2026-09-18; ONE VISUAL ITEM LEFT, AND IT CLEARS ITEM 28's BLOCKER] THE FOOTER NOW RESOLVES THE DASHBOARD A SESSION NAMES, so `C:\Projects\general\Vercel` (the workspace the dashboard skill declares is where every dashboard change runs from, 307 recorded Sessions) is no longer blank. Operator report that started it: "the footer has dashboard URL and design project and it only seems to work some of the time, it does not work when it is in the Vercel directory". Root cause, measured rather than assumed: the resolver read the workspace directory alone, and `matches()` accepts only a mapped folder, a folder inside it, or a trailing `packages/dashboards/src/<key>`, none of which the Vercel folder is. The fix consults the Session's OLDEST operator message only when the workspace names no dashboard, read at O(1) from the host-only `titleInput` projection the title service already folds, and matched against a key lookup inverted from the two existing maps (66 keys, 6 front-door hosts, no new file) as a `/dashboard <target>` target or a front-door address; an address counts only when BOTH its host and its key are known, so a client-site URL ending in a key is refused. The maps are also re-read when either file's mtime or size moves, which closes the "a map edit needs an app restart" note of 2026-09-17. The footer now renders NOTHING when neither value resolves (operator decision), superseding the tenth pass that kept two empty labels. Measured over the real stores: 761 recorded Sessions resolve 85 -> 132 and the 341 listed Sessions resolve 67 -> 106, the Vercel folder going 0 -> 29 of 77. Commit `43500cd452` on `fix/session-footer-intent`, fast-forward merged into `update/v0.1.5-rc.2`, both refs pushed. Installer 194,931,936 bytes, 2026-09-17 19:16:27, all four new markers proven inside the packaged seed archives. THE INSTALLER WAS THEN REBUILT to carry a second change in the same install: the sidebar's folder rows now sort by name (operator request, commit `a21af3a222`, 216 ui-workspace tests, registry 23 rows with `workspace-folder-sort`), so the build to run is `deepseek-harness-0.1.5-rc.2-win-x64.exe`, 194,901,646 bytes, 2026-09-17 23:52:45, and the 19:16 build is SUPERSEDED. Markers re-proven in the newer seed archives: `byWorkspaceName` (3), `resolveSessionLinks` (2), `hasWorkspaceLinks` (2). INSTALLED 2026-09-18 at 09:58 and verified live: installed exe 244,440,576 bytes written 2026-09-17 23:52:40; provision log `provision-2026-09-18T13-58-39-011Z.log` ends `staged health check passed` (14:04:39Z) / `staging profile activated as 0.1.5-rc.2` / `applyRelease finished`; four processes from 09:58:36; the extracted profile carries `resolveSessionLinks` (2), `hasWorkspaceLinks` (2) and `byWorkspaceName` (3); `dsh_local_features_check.py` reads 23 of 23. SEEN ON SCREEN: the folders under the expanded SIG group read Agents, backlinks, blog-articles, client-reporting, content-planner, Google Business, onboarding, service-pages (case-insensitive A to Z), and the Session on screen (which maps to nothing) shows NO footer at all, which is the new quiet behaviour. WHAT IS STILL OWED IS ONE PHOTOGRAPH: a POPULATED footer, `Dashboard: <url>` plus `Design Project: <name>`, which needs one click on a Session whose workspace resolves (the `Dashboard Design ...` row in All Sessions, or any Vercel Session). Evidence folder `C:\Projects\logs\2026-09-18\dsh-footer-intent\`. WHAT IS LEFT: (a) Steve runs `finish-install.ps1` from `C:\Projects\worktrees\dsh-footer-intent` in a NEW PowerShell window, because it force-closes the app and cannot be run from inside a Session; (b) read the markers out of the running profile, the newest provision log, the process list and the feature check; (c) PHOTOGRAPH the footer of an OPEN Session, which item 28 has owed since 2026-09-13 and which is finally reachable, because a Vercel Session whose first message is `/dashboard <key>` now resolves. KNOWN LIMITS, stated rather than hidden: a Session whose first message never names a dashboard still shows no footer, and a cold Session still resolves by folder alone because the list path never opens a cold log.
28. [UNVERIFIED, 2026-09-13] Session footer visual rendering, SECOND ROUND. The 2026-09-12 install had been packaged from `update/v0.1.5-rc.2` WITHOUT PR #21, so `dsh_local_features_check.py` read `workspace-unarchived-count` and `session-footer` as MISSING (13/15), and the count badges were gone from the sidebar. PR #21 is now merged (`5512545eac`) and a rebuilt installer carries it (`e39f7bf01e`); after the operator installs, confirm 18/18 markers, then EYEBALL the footer (summary plus the conditional dashboard link), the workspace count badge, and a long question card. The on-screen rendering has still never been seen.

## 2026-09-13 - pruned by dsh-settings-retention save

Moved out of OPEN_ISSUES.md. Original wording preserved verbatim.

29. [RESOLVED 2026-09-13] ds-harness-update Step 8 housekeeping for the 0.1.5-rc.2 UI build: the session-footer / workspace-indent / unarchived-count work, plus the four 2026-09-13 fixes, are recorded in the playbook and the state files. See the 2026-09-13 sections of CURRENT_STATE.md and VERIFICATION_RESULTS.md.

## 2026-09-12 - pruned by dsh-session-footer-ui save

13. [RESOLVED 2026-09-11] Windows packaging needs two release settings that were stored nowhere in the repo: DSH_DESKTOP_APP_ID and DOWNLOAD_TEST_ORIGIN (https://download.neotech.biz). Without them electron-builder fails before it builds. THE APP ID RECORDED HERE WAS WRONG: it said com.neotechnetworks.deepseek-harness; the installed 0.1.5-alpha.1 was built with **com.deepseek.harness**. Settled by derivation, not by memory: electron-builder computes the NSIS uninstall key as uuid5(appId) in namespace 50e065bc-3134-11e6-9bab-38c9862bdaf3 (NsisTarget.js, ELECTRON_BUILDER_NS_UUID). com.deepseek.harness gives 7808434f-469e-5eba-848e-edf64d3b94ce, which is the live 0.1.5 uninstall entry; com.neotechnetworks.deepseek-harness gives 7260a3eb-fb49-5c0a-a594-ea7b31e1d959, the stale 0.1.3 entry. A wrong app id does NOT fail the build: it installs a second parallel copy and leaves the running app untouched. RESOLVED 2026-09-11: both are now recorded in-repo at `apps/desktop/PACKAGING.md`, together with the uuid5 derivation that makes the app id non-optional and the live uninstall key it must match. A checked-in `.env.example` is NOT possible on this repo: the pre-commit hook refuses that filename ("refusing to commit credential/settings files"), and `--no-verify` is not an acceptable trade for a documentation file.
24. [RESOLVED 2026-09-11, in 0.1.5-rc.2] The fork-local `vision-routing` plugin defaulted to the retired `deepseek-v4-flash-vision-exp`. The installed profile's `dsh-vision-routing-0.1.5-rc.1` package declares `DEFAULT_ROUTE = { provider: "deepseek-official", model: "deepseek-v4-flash-vision-exp" }`. Impact is nil today: the default session model is `deepseek-flash`, which declares image input itself, so the router is only reached from a text-only session model (`deepseek-v4-pro`), and the retired id still answers at Flash price. FIXED IN 0.1.5-rc.2 (branch update/v0.1.5-rc.2, commit 7fdd0d6c9c): `DEFAULT_ROUTE` is now `deepseek-flash`, both READMEs and the package tests follow, and the packaged seed archive was read back carrying 1 occurrence of `deepseek-flash` and 0 of the retired id. LIVE as of the 2026-09-11 install: the RUNNING profile's `dsh-vision-routing/lib/index.js` carries `deepseek-flash` once and the retired id zero times.

## 2026-09-11 - session dsh-version-trap-trunk-checkout (moved out of OPEN_ISSUES.md)

Moved out of OPEN_ISSUES.md. Original wording preserved verbatim.

- [was RESOLVED 2026-09-11] The right-sidebar per-session width fix (2026-09-09) was uncommitted in the primary checkout C:\Projects\repos\deepseek-harness (branch fix/account-usage-remote-mount). 11 files: ui-layout stores/service/AppFrame + ui-sidebar-right index/seat + their tests + the client api-catalog. 220 tests pass, client typecheck clean. RESOLVED 2026-09-11: the CHANGE is live and was never actually open. It was replayed onto the rc.1 line as `fceae1d6f4 fix(ui-layout): key right panel width per session`, and the running profile's `dsh-client-ui-layout/lib/client.js` carries `rightbarBySession` 7 times with the old single `rightbar` gone, so the shipped code has it. The 11 modified files sitting in the primary checkout are DUPLICATES of that already-live work, not pending work; do not commit or reset them from another session. What is still unproven is only the on-screen behaviour (independent widths per session), which needs Steve's eyes.
  CLOSED OUT 2026-09-11 (session dsh-version-trap-trunk-checkout): the duplicate files are gone from the primary checkout, which is now clean on `master`. They were saved to `~\.claude\Exports\2026-09-11_dsh-primary-checkout-uncommitted-delta.patch` (12 files, 47,852 bytes) first, then discarded, and the branch that held them is archived at `origin/fix/account-usage-remote-mount`. The only remaining part is the on-screen check above.

## 2026-09-08 - resolved during session account-usage-footer-readout (remote-mount fix)

Moved out of OPEN_ISSUES.md. Original wording preserved verbatim.

- [was UNVERIFIED, now VERIFIED] Live desktop smoke test of vision-routing auto-vision (added 2026-09-08): attach an image to a DeepSeek Pro or Flash session with the subagent-model-selection setting enabled and the vision model in the allowed list, and confirm the model receives the description instead of "Model does not support image input".
  RESOLVED: on DeepSeek-V4-Flash an attached PNG was replaced with `[Attached image description (vision model): A single solid red circle centered on a plain white background...]` and no raw image placeholder was left behind. Evidence: scratchpad screenshot `vision-textonly.png` for session 1fa89993.

- [was UNVERIFIED, now VERIFIED in part] On-screen check of the Claude Max usage readout (added 2026-09-08): the installer was built and installed at 19:10 and the app relaunched at 19:59 carrying the package in its extracted profile, so only the pixels are unproven. Open a session and confirm the two percentages sit beside the stats line under the composer and that clicking them opens the reset times.
  RESOLVED for the percentages: the composer dock renders `5h 37% - Week 24%` in a live session, and again in a session created after the reinstall. Evidence: scratchpad screenshot `app-booted.png`. The click-to-open reset panel was NOT exercised; it is carried forward as a new item in OPEN_ISSUES.md rather than being silently closed.

## 2026-09-08 - session-status triggers composed into the presets

- RESOLVED: "The save-state 'declare finished' step has no working agent trigger: set_session_status (tool-session-status) is not in the agent tool catalog this session (not composed in the standard preset agent plane) and /status is human-only" - composed `command-session-status` (/status) and `tool-session-status` (set_session_status) into the standard, cordis, and ptc presets (mirroring command-goal / tool-goal); added a shipped-preset completeness test asserting the two rows are composed and not disabled. 93/93 targeted tests pass.

## 2026-09-08 - session creation fixed (standard-hooks preset path)

- RESOLVED: "Session creation fails in the desktop app after launch: the default preset "standard-hooks" fails to mount with "Cannot find package" for roughly twenty built-in host plugins ... not declared as dependencies of @deepseek-ai/dsh-agent-presets, so they are never installed. Fix: declare those plugins as dependencies" - the diagnosis was wrong: all ~23 plugins ARE declared and installed in the packaged profile. The real cause was the `standard-hooks` user preset at `~/.dsh/.agent-presets/standard-hooks/agent.cordis.yml`, whose `cordis:include` path hardcoded the SOURCE repo (`C:/Projects/repos/deepseek-harness/...`) instead of the installed profile. Repointed it at `~/.dsh/profiles/desktop/node_modules/@deepseek-ai/dsh-agent-presets/presets/standard/agent.cordis.yml`; New Session now works (user confirmed).

## 2026-09-08 - dependency gate fix

- RESOLVED: "Session-status work from the prior session is still uncommitted on `deepseek-harness` and still fails `verify-package-dependencies`: missing `@deepseek-ai/dsh-session-status` in `packages/client/connection/package.json` and `@deepseek-ai/dsh-goal` in `packages/client/ui-workspace/package.json`" - added both as workspace:^ devDependencies and refreshed pnpm-lock.yaml; verify-package-dependencies now exits 0.

## 2026-09-08 - open-session-in-subfolder

- RESOLVED: "origin is `deepseek-ai/deepseek-harness` with no push access, so a fork target is needed before it can be pushed" - created the `NeoTech-Networks/deepseek-harness` org fork and pushed `feat/open-session-in-subfolder` and `master` there.
## 2026-09-08 - vision-routing auto-vision smoke test

- RESOLVED: "- [UNVERIFIED] Live desktop smoke test of vision-routing auto-vision (added 2026-09-08): attach an image to a DeepSeek Pro or Flash session with the `subagent-model-selection` setting enabled and the vision model in the allowed list, and confirm the model receives the description instead of "Model does not support image input"" - the fix is shipped and verified end-to-end. The operator attached an image to a DeepSeek Pro session and the model received ONLY `[Attached image description (vision model): ...]` (an accurate reading of the screenshot) with no `[image omitted ...]` placeholder. The feature shipped via `5fee33421f` + `41050689df`; the placeholder drop via `c1a9951d60`.

## 2026-09-08 - plan-mode-default-active

- RESOLVED: "deepseek-harness state-file sync fails: the save-state commit worker pushes to the upstream remote (`deepseek-ai/deepseek-harness`) where `neotechnet` has no write access, so every sync returns 403. The repo's `origin` points at upstream; the writable fork is `neotech` (`NeoTech-Networks/deepseek-harness`). Fix: point the worker's push (or `origin`) at the fork." - verified this session via `git remote -v`: `origin` now points at the `NeoTech-Networks/deepseek-harness` fork and `upstream` at `deepseek-ai/deepseek-harness`, so the worker's push to origin now lands on the writable fork.
