Continue deepseek-harness. Plan: `C:\Users\SteveDempsey\.claude\plans\dsh-shortcut-double-send.md`. State: this worktree's `CURRENT_STATE.md` (2026-09-11 section), `VERIFICATION_RESULTS.md` (2026-09-11 table) and `OPEN_ISSUES.md` (items 21 to 23).

Steve reported the Alt+P shortcut sending the serve-the-promote-phrase command twice. It is measured, not guessed: 26 doubles in 181 promote-phrase submissions across 357 session logs, 22 of them 3 to 10 ms apart, each pair carrying two distinct client request ids. The handler's only guard was a stale `locked || machineBusy` reading from the last render.

FIXED AND PUSHED: commit `a28a606f4c` on `fix/composer-shortcut-modifier` adds a module-scope 300 ms single-flight latch plus `if (event.repeat) return`, with 6 new cases in `input-bar.client.spec.tsx` (98/98). Typecheck, full build and packaging all exit 0. The guard is read back out of the packaged seed.

INSTALL IS PENDING AND IS STEVE'S. The rebuilt installer, `deepseek-harness-0.1.5-rc.1-win-x64.exe` (194,785,784 bytes, 2026-09-11 07:53), is at `C:\Projects\worktrees\dsh-shortcut-fix\apps\desktop\.desktop-build\targets\win-x64\artifacts\` but NOT installed. It was built with `DSH_DESKTOP_APP_ID=com.deepseek.harness`, which matches the live uninstall key `7808434f-...`, so the app-id conflict in item 13 is resolved and the installer's derived path now matches the installed release (both 0.1.5-rc.1).

Steve runs, from a NEW PowerShell window: `powershell -ExecutionPolicy Bypass -File "C:\Projects\worktrees\dsh-shortcut-fix\finish-install.ps1"`. After SETUP COMPLETE: `py C:\Claude\bin\dsh_local_features_check.py` must exit 0 (11 rows), read `CHORD_LATCH_MS` and `event.repeat` back out of the running profile's `dsh-client-ui-conversation/lib/client.js`, then ONE real Ctrl+Shift+P must send exactly one `deploy to production` with no Desktop Plugins window, and a two-second hold must still send exactly one.

TWO INSTRUMENT NOTES, so nobody repeats the wasted hours: (1) `C:\Claude\bin\dsh-alt-chord-probe\run-probe.ps1` failed on 2026-09-11 against Electron 44: it reported `activated=True` while focused on a handle with an EMPTY window title, and NO key event reached the renderer or `before-input-event`. A positive-control gate was added, so the script now exits 3 rather than typing the chords blind, but the focus assertion needs the window chosen by handle AND title. (2) The headless alternative, `count-submits.mjs` in the same folder, boots the real client and Host fine but cannot reach a live composer in a plain `dsh web` home: the shipped `directory-picker` row is the `-auto` chooser, which raises no dialog in the page (the web e2e scaffold disables it and mounts `-browse` instead, scaffold.ts lines 599 to 608). Patch that in, then the script gives the one-chord-one-prompt count pre-install.

Two packaging facts still to add to the playbook `C:\Projects\repos\playbooks\DeepSeek Harness\05-neotech-fork.md`: prepare-runtime's Node 24.17.0 fetch from nodejs.org can time out (seed `apps/desktop/.desktop-build/downloads\` from another worktree first), and the app id must be `com.deepseek.harness`.
