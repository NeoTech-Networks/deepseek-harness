Continue deepseek-harness. The right-sidebar per-session width fix is DONE but UNCOMMITTED in the primary checkout (branch fix/account-usage-remote-mount). 11 files: ui-layout (stores/service/AppFrame) + ui-sidebar-right (index/seat) + their tests + the client api-catalog. 220 tests pass, client typecheck clean, catalog gates pass. Next: commit on the right branch, then build + install via the ds-harness-update flow, and smoke-test that each session remembers its own panel width in the desktop app.
