Continue deepseek-harness: this session added plan-mode default-active (a `defaultActive` config plus `pinInitialPlanMode` in the plan-mode package), so every new session starts in plan mode on every model; live-verified after an app restart. Source committed on `feat/open-session-in-subfolder` and pushed to the `NeoTech-Networks` fork. Next: nothing required for this feature. Still open: the save-state "declare finished" step (`set_session_status` is not in the agent catalog), the leftover personal `neotechnet/deepseek-harness` fork, and several user-gated live desktop smoke tests. Details in C:\Projects\repos\deepseek-harness\CURRENT_STATE.md.
