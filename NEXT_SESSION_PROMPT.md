Continue deepseek-harness in the worktree C:\Projects\worktrees\dsh-status-icons-v015 (branch fix/session-status-icons-v015, cut from update/v0.1.5-alpha.1, which is the line the installed app actually ships from). The session status icon fix is built, packaged, installed and read back out of the running profile: the goal-to-status mapping now folds in session-status and expires like any declared status, derivePhase ranks a declared status below plan mode / running / running descendants, the live glyph takes the ongoing blue plus a pulse, and the right-hand Sessions panel uses the same vocabulary with a test holding the two derivations in agreement. Next: get Steve's visual pass on the sidebar (watch one session go working to finished and confirm the mark changes at the real boundary, not before), then the remaining user-gated GUI smoke tests, then the two branch-hygiene items (the fix exists only on a local branch, and two branches carry overlapping work on different bases). Details in C:\Projects\worktrees\dsh-status-icons-v015\OPEN_ISSUES.md.
