# Next session prompt

Continue deepseek-harness. The 0.1.5-rc.2 build is installed and running.

## Just fixed (2026-09-12)

The `llm-route-fallback` plugin was moving EVERY request from `deepseek-flash`
onto `deepseek-v4-pro`: its `limitBytes: 3000` sits below the real tool-catalog
size, so the "tools assembled" gate never kept a request, and every new session
flipped from flash to pro on its first message. Disabled at the source on
`feat/llm-route-fallback` (commit `9f8261490a`), rebuilt, installed, and verified
live. See the top of `CURRENT_STATE.md`.

## What is left

- (done 2026-09-12) `update/v0.1.5-rc.2` now carries the disabled row: the fix was
  replayed as `1e89100c7f` on top of the PR #20 squash-merge (`ba3644927a`) and
  pushed.
- Eyeball the session footer in the app: the summary line under the message box,
  and the dashboard link resolving when the session workspace is a dashboard
  source folder (map at `~/.dsh/dashboard-links.json`, regenerated by
  `py C:\Claude\bin\dsh_dashboard_links.py`).
- ds-harness-update Step 8 housekeeping: update the DeepSeek Harness playbook
  version-history table and CHANGES.md for this build. The error ledger row 30 is
  already added and pushed.
- Merge PR #21 when ready.

## Checkout state

`C:\Projects\repos\deepseek-harness` is on `master`. The route-fallback fix is on
branch `feat/llm-route-fallback` (worktree `C:\Projects\worktrees\dsh-route-fallback`),
one commit `9f8261490a` on top of `572ea54e41`. The UI work lives on branch
`feat/ui-session-footer` (worktree `C:\Projects\worktrees\dsh-ui-session-footer`),
two commits `ed2298f345` and `86ed07586b`.
