Continue deepseek-harness. THE TWO HARNESS FIXES ARE LIVE. Installed 2026-09-09 08:35, app relaunched 08:36, and both were read back out of the RUNNING profile: dsh-fs-local/lib/index.js carries PUBLISH_RETRY_DELAYS_MS, publishOverExisting and readTextBytesConfirmingBinary; dsh-plan-mode/lib/index.js carries describePlanFault with zero occurrences of the old "requires a non-empty markdown plan" message. OPEN_ISSUES item 0 is CLOSED. Do not redo the port, the build or the install.

TWO THINGS TO KNOW BEFORE YOU DIAGNOSE ANYTHING:

  1. "It crashed" after an install is the install WORKING. finish-install.ps1 step 1 is taskkill /F, the only way to close this app, so Windows and any session inside it report a crash. It was mistaken for a failed install once already, after an install that had fully succeeded. The script now warns before the kill. Whether an install worked is decided by the seed-integrity assertion and the post-install read-back, never by that message.

  2. STATE FILES ARE STILL BEING DESTROYED, and the 2026-09-08 writer fix is NOT the culprit (its read-failure branch skips instead of overwriting; the code was re-read to confirm). On 2026-09-09 at 08:18 CURRENT_STATE.md was found at 38 lines, three stacked marker blocks and zero content, against 251 in git. Recovered with `git checkout HEAD -- CURRENT_STATE.md`; the damaged copy is at %TEMP%\CURRENT_STATE.damaged.md. The mechanism that fits: two per-session worktrees under .claude/worktrees/ hold their OWN CURRENT_STATE.md at 80 and 68 lines, cut from older checkouts, so publishing a worktree copy over the primary deletes everything written since. This is OPEN_ISSUES item 16, now the top open item. BEFORE AND AFTER any state-file edit, compare the local line count against `git show origin/master:<file>`.

WHAT IS ACTUALLY LEFT, in priority order:
  - Item 16: stop worktree-based state publishing, or route every publish through C:\Claude\bin\state_file_reconcile.py (it unions, it does not replace). This is the one that keeps costing real history.
  - Items 1, 2, 5, 10: the CODE is proven live (every feature package is present in the running profile and the accountUsage remote mount is in the running client bundle). What is unproven is the ON-SCREEN behaviour: workspace group headers, Alt+S / Alt+P shortcuts and the right sidebar default, session status icons, the plan-mode pulse and the save-state Finished check. These need Steve's eyes, not another file read.
  - Item 18: re-run C:\Claude\bin\dsh_session_audit.mjs after a full day of real use to confirm the hook latency win holds on production traffic.
  - Item 19: 16 tool results lost in two process-wide bursts on 2026-09-08, cause still unattributed.
  - Item 8: DeepSeek vision needs a GUI image ATTACH. read_image on a path is not the test and will always refuse.
  - Item 13: record DSH_DESKTOP_APP_ID (com.deepseek.harness) and DOWNLOAD_TEST_ORIGIN (https://download.neotech.biz) in apps/desktop/README.md or a checked-in .env.example, so they stop being recovered from transcripts.

Full list in OPEN_ISSUES.md. Build worktree is C:/Projects/worktrees/dsh-update-v015 on update/v0.1.5-alpha.1 (pushed). The playbook at C:\Projects\repos\playbooks\DeepSeek Harness\05-neotech-fork.md is current as of this session and carries a 15-row error ledger.
Continue deepseek-harness: console windows no longer flash. Branch fix/hide-windows-consoles (pushed to the fork, not merged) hides all four unhidden Node launches plus both restricted-token creates, and it is the build currently installed (0.1.5-alpha.1 from C:\Projects\worktrees\dsh-hide-consoles). Next: decide whether that branch merges into the local feature stack, then work the pre-existing gate failures from earlier untracked work (sessions-panel, sidebar-explorer, session-status, vision-routing, plan-mode doc drift) and the remaining user-gated GUI smoke tests. Details in C:\Projects\worktrees\dsh-hide-consoles\OPEN_ISSUES.md and the session page C:\Projects\logs\2026-09-09\dsh-console-window-hide\SESSION_STATE.md.
