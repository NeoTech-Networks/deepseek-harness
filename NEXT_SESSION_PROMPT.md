# Next session prompt

Continue deepseek-harness. The 0.1.5-rc.2 build with the three UI changes is
installed and running: all 15 local features verified via
`dsh_local_features_check.py`. PR #21 (`feat/ui-session-footer`) is open into
`update/v0.1.5-rc.2`.

## What is left

- Eyeball the session footer in the app: the summary line under the message box,
  and the dashboard link resolving when the session workspace is a dashboard
  source folder (map at `~/.dsh/dashboard-links.json`, regenerated by
  `py C:\Claude\bin\dsh_dashboard_links.py`).
- ds-harness-update Step 8 housekeeping: update the DeepSeek Harness playbook
  version-history table, error ledger, and CHANGES.md for this build.
- Merge PR #21 when ready.

## Checkout state

`C:\Projects\repos\deepseek-harness` is on `master`. The UI work lives on
branch `feat/ui-session-footer` (worktree `C:\Projects\worktrees\dsh-ui-session-footer`),
two commits `ed2298f345` and `86ed07586b` on top of the route-fallback merge.
