Continue deepseek-harness. The "All Sessions" sidebar section is BUILT AND PACKAGED but NOT YET CONFIRMED INSTALLED. Feature: a new `sidebar.allSessions` slot adds a collapsible "All Sessions" section above Workspaces, listing every unarchived session newest-first with its workspace name and live status icon; clicking a row opens the session. Commit 6eb1341b69 on feat/sidebar-all-sessions, worktree C:/Projects/worktrees/dsh-all-sessions, one commit on top of update/v0.1.5-alpha.2.

Installer: C:/Projects/worktrees/dsh-all-sessions/apps/desktop/.desktop-build/targets/win-x64/artifacts/deepseek-harness-0.1.5-alpha.2-win-x64.exe (194,723,910 bytes). Steve has the install line (finish-install.ps1 in the same worktree). AFTER he installs: confirm the section renders above Workspaces, expands and collapses, and clicking a row opens the session; then run dsh_local_features_check.py and add an "all-sessions" marker row to C:\Claude\bin\dsh_local_features.json. Gates already green: typecheck, 69 tests, lint, catalog, note/README gates.

The two update-survival guards still apply after any install (dsh_local_features_check.py and dsh_config_vault.py verify). The top open issue is still 16/16-root-cause: no state-sync PR can merge on this fork because two inherited upstream workflows call create-github-app-token against owner deepseek-harness (a decision for Steve). Installer app id is com.deepseek.harness (item 13 corrected); DOWNLOAD_TEST_ORIGIN is https://download.neotech.biz.
