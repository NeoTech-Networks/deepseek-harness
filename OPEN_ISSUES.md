1. [UNVERIFIED] Live desktop smoke test of the workspace group header change (2026-09-08): launch the app and confirm the group section headers render in darker blue and the named groups sort A-to-Z.
2. [OPEN] Live desktop smoke test of this session's changes (Alt+S / Alt+P composer shortcuts and right-sidebar-hidden-by-default): both committed (88a2289cc3) and patched into the installed profile plus ~/.dsh/settings.yaml (autoOpen: false), but need a full app restart to confirm; user last reported the sidebar still auto-opening.
3. [OPEN] deepseek-harness state-file sync diverges: the save-state commit worker reports the state files (CURRENT_STATE.md, VERIFICATION_RESULTS.md, NEXT_SESSION_PROMPT.md) diverged from origin/master and could not be merged (exit 2 needs_attention, 2026-09-08). Fix: one-time manual union-merge of the diverged files (both sides kept, newest first), then re-run the worker. STILL FAILING as of 2026-09-09: five more exit-2 receipts for this repo, and eight more across C:/Claude and jetway-tracktik-paycom-sync.
4. [UNVERIFIED] Live desktop smoke test of the new quit confirmation (added 2026-09-08 in apps/desktop/src/main.ts): relaunch via launch-desktop.vbs, X the main window, and confirm the \"Are you sure you want to quit?\" dialog appears with Quit / Cancel behaving correctly.
5. [UNVERIFIED] Live desktop smoke test of session status icons: run launch-desktop.ps1, then /status waiting-production, stuck, finished, clear, and confirm the sidebar glyphs plus the row-menu Set status / Clear status.
6. [OPEN] Live desktop smoke test of the Files-tab \"New session here\" and \"New session in each sub-folder...\" gestures has not been run (user-gated GUI).
7. [OPEN] Personal fork neotechnet/deepseek-harness (created in error) could not be deleted: the gh token lacks the delete_repo scope. Delete it in the GitHub UI, or run gh auth refresh -h github.com -s delete_repo and re-run.
8. [OPEN] Several repo gates fail on untracked work in progress left by earlier sessions, unrelated to the usage readout: the sessions-panel package has no tsconfig path alias and its explorer sibling carries hard-coded UI strings and non-hairline borders, the session-status packages have a files-list and README section-order fault, vision-routing has no Model Experience section and hard-wrapped README prose, and the plan-mode doc block is out of date with its source.
9. [UNVERIFIED] Live desktop smoke test of the plan-mode pulse and the save-state \"Finished\" icon: full build is done (exit 0) and the animation is confirmed in the built bundle; relaunch via launch-desktop.vbs, run a /plan task to watch the icon pulse, then a /save-state for the green Finished check.
10. [OPEN] The accountUsage remote-mount fix is on branches only, not on master. fix/account-usage-remote-mount-only holds the isolated 5-line fix (b99e942c0b) on top of d14e69cfa9; fix/account-usage-remote-mount holds the rebased work in progress (78d5d95934); backup/wip-pre-rebase-87607362 is the pre-rebase copy. Decide whether to merge and delete the backup.
11. [UNVERIFIED] Clicking the two usage percentages in the composer dock to open the reset-times panel. The percentages themselves render (5h 37% - Week 24%, proven 2026-09-08); the click-to-open panel was never exercised. Carried forward from the item archived this session.
12. [OPEN] Windows packaging needs two release settings that are stored nowhere in the repo: DSH_DESKTOP_APP_ID (com.neotechnetworks.deepseek-harness) and DOWNLOAD_TEST_ORIGIN (https://download.neotech.biz). Without them electron-builder fails before it builds. Recovered from an old session transcript on 2026-09-08. Fix: record them in apps/desktop/README.md or a checked-in .env.example.
13. [OPEN] Packaging fails intermittently with EPERM renaming win-unpacked.tmp to win-unpacked (electronGet.js:187 renames with no retry). A manual rename seconds later succeeds, so it is a transient lock, most likely the on-access antivirus scanning the freshly extracted electron.exe. It cost two of four packaging attempts on 2026-09-08. Fix: retry the rename, or exclude apps/desktop/.desktop-build from on-access scanning.
14. [OPEN] Concurrent sessions share the primary deepseek-harness checkout and write to each other's work. On 2026-09-08 sessions restored inside the desktop app resumed autonomous builds in the same folder another session was packaging in (breaking two builds), and one committed another session's staged files into an unrelated wip commit on that session's branch. Fix: give each session its own worktree, per the One Worktree Per Session rule.
