0. [RESOLVED 2026-09-09, VERIFIED IN THE RUNNING CODE] Both fixes are LIVE. Steve installed at 08:35 and the app relaunched at 08:36; the profile was re-extracted with 247 packages. Read directly out of the running profile afterwards: `dsh-fs-local/lib/index.js` now carries `PUBLISH_RETRY_DELAYS_MS` (137), `publishOverExisting` (181, 652) and `readTextBytesConfirmingBinary` (449, 463, 471), and `dsh-plan-mode/lib/index.js` carries `describePlanFault` (59, 305, 474) with ZERO occurrences of the old `requires a non-empty markdown plan` message. One install directory on disk, no crash events in the Windows Application log. THE "CRASH" THE INSTALL REPORTS IS EXPECTED: `finish-install.ps1` step 1 is a `taskkill /F`, so Windows and any session inside the app report a crash; that is the forced close working, not the install failing, and the script now announces it before doing it. The four items this issue cast doubt on were re-checked the same way: every feature package (`dsh-workspace`, `dsh-session-status`, `dsh-vision-routing`, `dsh-account-usage`, `dsh-client-ui-sessions-panel`, `dsh-win32-process` carrying the CREATE_NO_WINDOW flag, `dsh-goal`) is present in the running profile and the accountUsage remote mount is in the running client bundle, so the CODE is live; only the on-screen behaviour of items 1, 2, 5 and 10 is still unproven and that needs eyes, not another read. Also cleaned up: the stale duplicate Add/Remove Programs entry left by the old app id (`7260a3eb...`) was backed up to `~\.claude\Exports\2026-09-09_dsh-stale-uninstall-key-backup.reg` and removed; one entry remains.

0-build. [HOW IT WAS BUILT, kept for the record] Port: `git cherry-pick 92e043bf4d` onto `update/v0.1.5-alpha.1` in worktree `C:/Projects/worktrees/dsh-update-v015`, clean, as `7c577fb6fe`; the four touched files had diverged by exactly one non-overlapping line. Proven: fs-local 142 passed with the 13 pre-existing POSIX failures unchanged and all 6 new cases green, plan-mode 94/94, typecheck exit 0, build exit 0, packaging exit 0, and the two packaged seed `.tgz` archives read back with `publishOverExisting` / `describePlanFault` present and the old `requires a non-empty markdown plan` message gone. Installer: `apps/desktop/.desktop-build/targets/win-x64/artifacts/deepseek-harness-0.1.5-alpha.1-win-x64.exe`, 190,730,194 bytes, packaged with `DSH_DESKTOP_APP_ID=com.deepseek.harness`. WHAT IS LEFT: Steve runs `finish-install.ps1` from a separate PowerShell window, then the next session reads `~/.dsh/profiles/desktop/node_modules/@deepseek-ai/dsh-fs-local/lib/index.js` and `.../dsh-plan-mode/lib/index.js` for the new symbols and submits one blockquote-first plan. Only then does this close. The doubt this item casts on items 1, 2, 5 and 10 is unchanged.

0-history. [ORIGINAL TEXT, kept for the record] THE HARNESS FIXES IN COMMIT 92e043bf4d ARE NOT RUNNING. The desktop app runs published 0.1.5-alpha.1; this checkout is 0.1.3-alpha.2. `pnpm run start:desktop` re-provisions ~/.dsh/profiles/desktop from 0.1.5 on every launch and overwrites any lib patched into it, so the "sync the built lib into the profile" technique used by several earlier sessions does NOT survive a restart. Verified by reading the running code after the relaunch: fs-local lib/index.js still has `if (!isENOENT(error)) throw error;` at lines 165 and 184 plus 3 unguarded "binary file" sites, and plan-mode lib/index.js line 270 still has the old `/^#\s+\S/` test. Fix: port the two fixes onto the 0.1.5 line (a worktree already exists at C:/Projects/worktrees/dsh-update-v015 on branch update/v0.1.5-alpha.1, which does NOT contain them), then build and install the installer. Impact while unfixed: a write to a file another process holds open can still be lost silently, and every plan written in this operator's own plan format is rejected once. The ROOT CAUSE of the state-file loss (the non-atomic writer) IS fixed and live, so these two are defence in depth rather than the primary fix. THIS ALSO CASTS DOUBT ON ITEMS 1, 2, 5 AND 10: any earlier session that claimed a fix was live because it synced a lib into the profile should be re-checked against the running code the same way.

1. [UNVERIFIED] Live desktop smoke test of the workspace group header change (2026-09-08): launch the app and confirm the group section headers render in darker blue and the named groups sort A-to-Z.
2. [OPEN] Live desktop smoke test of the Alt+S / Alt+P composer shortcuts and right-sidebar-hidden-by-default: both committed (88a2289cc3) and patched into the installed profile plus ~/.dsh/settings.yaml (autoOpen: false), but need a full app restart to confirm; user last reported the sidebar still auto-opening.
3. [RESOLVED 2026-09-09] State-file sync divergence. Root cause: the state files were committed on a long-lived feature branch that never reached origin/master, so they differed from it by insertions AND deletions (181/39 lines here), and the worker's 3-way apply can only apply appends. A contributing cause was found and fixed the same session: memory_save_state_actor.py overwrote a whole state file with just the marker block whenever a read failed transiently, which is the likely source of the deletions. Fixed by C:\Claude\bin\state_file_reconcile.py (union by section for logs, by row for table ledgers, snapshots take local, aborts rather than drop anything), run over all three repos. Verified: git_maintenance_worker.ps1 -StatusFilesOnly now logs "status files already match origin/master, nothing to commit" and "0 needing attention" for deepseek-harness (02:02:40Z), jetway-tracktik-paycom-sync (02:04:06Z) and C:/Claude (02:06:31Z). 431 rows and 26 sections of previously lost history were recovered into C:/Claude alone.
4. [UNVERIFIED] Live desktop smoke test of the quit confirmation (added 2026-09-08 in apps/desktop/src/main.ts): relaunch via launch-desktop.vbs, X the main window, and confirm the "Are you sure you want to quit?" dialog appears with Quit / Cancel behaving correctly.
5. [UNVERIFIED] Live desktop smoke test of session status icons: run launch-desktop.ps1, then /status waiting-production, stuck, finished, clear, and confirm the sidebar glyphs plus the row-menu Set status / Clear status.
6. [OPEN] Live desktop smoke test of the Files-tab "New session here" and "New session in each sub-folder..." gestures has not been run (user-gated GUI).
7. [OPEN] Personal fork neotechnet/deepseek-harness (created in error) could not be deleted: the gh token lacks the delete_repo scope. Delete it in the GitHub UI, or run gh auth refresh -h github.com -s delete_repo and re-run.
8. [UNVERIFIED, operator-gated] Live desktop smoke test of vision-routing: ATTACH an image to a DeepSeek Pro or Flash session and confirm the model receives a description. Checked 2026-09-09: the vision-routing row IS present in the installed web-app cordis patch and the package is installed in the running profile, so the wiring is in place and only the on-screen behaviour is unproven. NOTE FOR WHOEVER TESTS THIS: calling read_image on a file path is NOT the test and will always fail with `model "deepseek-v4-pro" does not declare image input`. That refusal is correct. Vision-routing acts at PROMPT ADMISSION on attached images, not on the read_image tool, so a headless read_image probe proves nothing either way. A probe on 2026-09-09 produced exactly that error and was briefly mistaken for a defect.
9. [OPEN] Several repo gates fail on untracked work in progress left by earlier sessions: the sidebar-explorer package carries hard-coded UI strings and non-hairline borders, the session-status packages have a files-list and README section-order fault, vision-routing has no Model Experience section and hard-wrapped README prose, and the plan-mode doc block is out of date with its source.
10. [UNVERIFIED] Live desktop smoke test of the plan-mode pulse and the save-state "Finished" icon: full build is done (exit 0) and the animation is confirmed in the built bundle; relaunch via launch-desktop.vbs, run a /plan task to watch the icon pulse, then a /save-state for the green Finished check.
11. [OPEN] The accountUsage remote-mount fix is on branches only, not on master. fix/account-usage-remote-mount-only holds the isolated 5-line fix (b99e942c0b) on top of d14e69cfa9; fix/account-usage-remote-mount holds the rebased work in progress; backup/wip-pre-rebase-87607362 is the pre-rebase copy. Decide whether to merge and delete the backup.
12. [UNVERIFIED] Clicking the two usage percentages in the composer dock to open the reset-times panel. The percentages themselves render (5h 37% - Week 24%, proven 2026-09-08); the click-to-open panel was never exercised.
13. [RESOLVED 2026-09-11] Windows packaging needs two release settings that were stored nowhere in the repo: DSH_DESKTOP_APP_ID and DOWNLOAD_TEST_ORIGIN (https://download.neotech.biz). Without them electron-builder fails before it builds. THE APP ID RECORDED HERE WAS WRONG: it said com.neotechnetworks.deepseek-harness; the installed 0.1.5-alpha.1 was built with **com.deepseek.harness**. Settled by derivation, not by memory: electron-builder computes the NSIS uninstall key as uuid5(appId) in namespace 50e065bc-3134-11e6-9bab-38c9862bdaf3 (NsisTarget.js, ELECTRON_BUILDER_NS_UUID). com.deepseek.harness gives 7808434f-469e-5eba-848e-edf64d3b94ce, which is the live 0.1.5 uninstall entry; com.neotechnetworks.deepseek-harness gives 7260a3eb-fb49-5c0a-a594-ea7b31e1d959, the stale 0.1.3 entry. A wrong app id does NOT fail the build: it installs a second parallel copy and leaves the running app untouched. RESOLVED 2026-09-11: both are now recorded in-repo at `apps/desktop/PACKAGING.md`, together with the uuid5 derivation that makes the app id non-optional and the live uninstall key it must match. A checked-in `.env.example` is NOT possible on this repo: the pre-commit hook refuses that filename ("refusing to commit credential/settings files"), and `--no-verify` is not an acceptable trade for a documentation file.
14. [OPEN] Packaging fails intermittently with EPERM renaming win-unpacked.tmp to win-unpacked (electronGet.js:187 renames with no retry). A manual rename seconds later succeeds, so it is a transient lock, most likely on-access antivirus scanning the freshly extracted electron.exe. It cost two of four packaging attempts on 2026-09-08. Fix: retry the rename, or exclude apps/desktop/.desktop-build from on-access scanning.
15. [OPEN] Concurrent sessions share the primary deepseek-harness checkout and write to each other's work. On 2026-09-08 sessions restored inside the desktop app resumed autonomous builds in the same folder another session was packaging in (breaking two builds), and one committed another session's staged files into an unrelated wip commit on that session's branch. Fix: give each session its own worktree, per the One Worktree Per Session rule.
16. [OPEN, SHARPENED 2026-09-09 BY A LIVE RECURRENCE, RAISED IN PRIORITY] A MECHANISM IS NOW NAMED. At 08:18 CURRENT_STATE.md in the primary checkout was found at 38 lines: three stacked save-state marker blocks and zero dated sections, against 251 lines in git HEAD and origin/master. Recovered whole with `git checkout HEAD -- CURRENT_STATE.md`; the damaged copy is at %TEMP%\CURRENT_STATE.damaged.md if anyone wants to diff it. THE 2026-09-08 WRITER FIX IS NOT THE CULPRIT: its read-failure branch now skips instead of overwriting, and the code was re-read this session to confirm that. The mechanism that fits the evidence is worktree staleness: `.claude/worktrees/session-1fa89993-3cf` and `session-c7848c36-f3a` each hold their OWN CURRENT_STATE.md, at 80 and 68 lines, built on the old checkout each worktree was cut from. Anything that publishes a worktree's copy over the primary or over master therefore DELETES every section written since that worktree was created, and a file that has been through that more than once ends up as nothing but stacked marker blocks, which is exactly the shape found. The other two repos were checked the same morning and are intact (C:/Claude 423 lines, jetway-tracktik-paycom-sync 507). Fix candidates, in order of preference: never let a per-session worktree publish a state file, publish only through `state_file_reconcile.py` which unions rather than replaces, or stop cutting worktrees that carry state files at all. Until then, ANY session that edits a state file should check its line count against `git show origin/master:<file>` before and after. RECURRED A THIRD TIME 2026-09-11: CURRENT_STATE.md was again found at 38 lines in the primary checkout holding nothing but the stacked-marker shape, against 728 in origin/master, and VERIFICATION_RESULTS.md was 148 lines short of origin/master in the same checkout. Both restored from origin/master; damaged copies archived to %TEMP% as deepseek-harness-CURRENT_STATE.damaged-2026-09-11.md and deepseek-harness-VERIFICATION_RESULTS.damaged-2026-09-11.md. Note the direction changed: a recovery from HEAD is no longer enough, because HEAD itself is 287 lines while origin/master holds 728, so restore from origin/master. Three occurrences make publishing through `state_file_reconcile.py` the default rather than an option.

16-root-cause. [2026-09-09, THE ENGINE BEHIND THE WHOLE LOOP, FOUND] State files can never reach `origin/master` on this fork, which is why every checkout diverges and why a stale copy published over the primary always destroys real history. The git maintenance worker does its job correctly: it opens a status-sync PR (currently #6, `chore/state-sync-2026-09-09-163027`, opened 16:31:48Z) and then refuses to merge it because two required checks fail. Those two checks are INHERITED UPSTREAM WORKFLOWS THAT CANNOT PASS HERE. `gh run view --job 102554029594 --log-failed` gives the reason verbatim: `Error: The 'client-id' (or deprecated 'app-id') input must be set to a non-empty string.` Both `Issue policy` and `Issue lifecycle` call `actions/create-github-app-token` against `owner: deepseek-harness` with a GitHub App credential that exists only in the upstream organization, so on `NeoTech-Networks/deepseek-harness` they fail in 9 seconds every time, on every PR, forever. Everything else on #6 is green or pending (`node 26`, `node 24.9`, `Pack npm tarballs`, the python matrix all pass). CONSEQUENCE: no state-sync PR has ever merged, `origin/master` has not moved since the fork's own history, every worktree keeps drifting further from it, and the worker's 3-way apply then conflicts on all four files (logged repeatedly at 03:48-03:49Z: "4 status file(s) still conflicted after 3 retr(ies)"). FIX IS A DECISION, NOT HOUSEKEEPING, because it changes what gates a merge on `master`: either stop those two inherited workflows running on this fork, or drop them from the required set, or point the worker's status PRs at a branch those checks do not gate. Until one of those is chosen, expect the destruction to keep recurring no matter how many times the file is recovered.

16-second-recurrence. [2026-09-09, MEASURED TWICE IN ONE SESSION, WRITER RULED OUT] It happened again, twice, while the 0.1.5-alpha.2 build was running, and this time the timeline is exact. 11:56 local: primary CURRENT_STATE.md found at 22 lines against 287 in HEAD and origin/master, recovered whole with `git checkout HEAD --` (damaged copy at %TEMP%\CURRENT_STATE.damaged-2026-09-09-1156.md). 12:31 local: found again at 38 lines, holding save-state blocks dated 2026-09-08T17:47, 2026-09-09T00:08 and 2026-09-09T16:11Z, the newest written by session 17aa0b40-6ead-424a-9dbf-e091b2976b35, which is not this session; recovered again to 287 (damaged copy at %TEMP%\CURRENT_STATE.damaged2-2026-09-09-1231.md). THE SAVE-STATE WRITER IS NOT THE CULPRIT AND CAN BE STRUCK OFF: `apply_marker_block` in memory_save_state_actor.py was re-read this session at lines 1030-1079, and it has exactly three branches - create when missing, regex-replace the one marker block in place, or append at the end - none of which can shorten a file, and its read-failure branch now skips instead of overwriting. Something is PUBLISHING A DIFFERENT COPY over the primary, which is item 16's worktree-staleness theory holding up. The landmines were counted the same minute: of the 10 checkouts on this machine carrying CURRENT_STATE.md, 8 hold stale short copies (80, 68, 106, 119, 135, 98, 98 lines) and only the primary, its own path alias and the git-maintenance temp worktree hold 287. Any process that publishes one of those 8 destroys the file. Next step for whoever picks this up: identify the publisher (the git maintenance worker and /save-state are the two candidates that write outside their own worktree) rather than recovering a third time.

16-history. [ORIGINAL TEXT] The structural cause behind item 3 is still live: deepseek-harness state files are written on branch fix/account-usage-remote-mount and C:/Claude's on contract-first-workstation, while the worker merges onto origin/master. The reconcile realigned them once and it held, but editing a state file on the feature branch re-diverged it within minutes on 2026-09-09 and needed another reconcile run. Fix: settle the branch situation, or teach the worker to reconcile instead of only appending.
16b. [OPEN, low, found 2026-09-09] `state_file_reconcile.py` REORDERS a table-ledger file even when it recovers nothing. Run against VERIFICATION_RESULTS.md with "+0 recovered from origin/master", which should have been a no-op, it moved a section heading to the bottom of the file and merged that section's table into the section above it. No content was lost, but the result misattributes one section's verification rows to another, which is worse than useless in a verification ledger. The working tree was restored from HEAD; origin/master still holds the reordered copy, so local and master now differ in ORDER only. Fix: make the union stable when the recovered-row count is zero, or teach it that a `##` heading binds the rows beneath it. CONFIRMED AGAIN 2026-09-11: a `--repo` DRY RUN reported "+0 recovered from origin/master" on every log (`CURRENT_STATE.md` and `state-archive/CURRENT_STATE_history.md` 41 and 26 sections, `VERIFICATION_RESULTS.md` 346 rows), which is precisely the no-op condition above, so `--apply` was deliberately NOT run. Until the union is proven stable at zero recovered rows, treat this tool as dry-run-only on this repo. That leaves the divergence it exists to heal open, but per item 16-root-cause no state-sync PR has ever merged, so writing a union to origin/master would buy nothing except the reorder risk.
17. [OPEN, harness] A 429 carrying Z.ai's entitlement error (code 1311, "your current subscription plan does not yet include access to GLM-5.3-Highspeed") is classified as RATE_LIMIT and retried five times with backoff, burning ~19 seconds before failing. It is permanent, not transient. glm-5.3-highspeed has been removed from subagent-model-selection.allowedModels so nothing can route to it, but the misclassification remains for any future entitlement 429.
18. [FIXED 2026-09-09, pending a day of real-use data] Hook latency. Measured over 52 sessions: 11,019 invocations, 353 minutes of wall time, 1,829ms average per PreToolUse. The hooks were never the cause: no individual guard measured worse than 142ms, and only 3 of 21 PreToolUse commands even match a read. FOUR causes found and fixed in the bridge: (a) pool width was min(16,N) against a 22-hook fan-out, costing an extra wave on the busiest event; (b) shell=True spawned cmd.exe as well as the interpreter for every hook, 553ms to 214ms over 22 cold starts in an interleaved A/B; (c) the transcript projection re-decompressed the WHOLE session log every call, 64.5ms to 0.2ms steady state on a 2.8MB log; (d) session_route did the SAME full decompress a second time, purely to read two strings, at 0.671s of a 1.002s run. The important result is the SHAPE, not the milliseconds: cost used to grow with session length, which is why long sessions degraded through the day. Now measured at 461ms on a 100KB log and 483ms on a 2.78MB log, so it is effectively flat in log size, down from a median 1,113ms at the start of the day. Correctness held: the incremental decode is byte-identical to a full rebuild, a full decode consumes every source byte, and the computed route matches a reference recomputed the old way from the compressed source. NEXT: re-run bin/dsh_session_audit.mjs after a day of real use to confirm the fleet-wide average moves, since every number above is a bench measurement rather than production traffic.

18a. [OPEN, low] The dsh-hook-bridge test test_every_configured_hook_is_listed_in_policy fails, and failed identically before any 2026-09-09 change (confirmed by stashing the change and re-running). Two configured hooks are absent from the bridge's policy.json: skill_frontmatter_guard.py and auto_state_commit_dispatch.py. Left alone deliberately rather than blind-edited, because policy.json governs the enforcement layer and the gap is unrelated to the latency work.
19. [OPEN, harness] Sixteen tool results were lost on 2026-09-08 to "interrupted after it was recorded, but no result was durably recorded", in two process-wide bursts (three sessions at 14:26:34/36/38 local, two at 14:40:21/29). The affected tools were exclusively long-blocking or interactive ones (pwsh 7, ask_user_question 3, exit_plan_mode 2, todo_write 1). Simultaneity across independent sessions rules out per-session aborts. Cause not yet attributed to a restart, a crash or a shared abort path.
20. [RECORDED 2026-09-09, no action] Three tool calls that day arrived with damaged arguments (two with a stray "and" inside the first string value, one missing ~6,000 leading characters). This is NOT a harness fault and the accumulator was deliberately left alone: assistant/message already records the raw provider stream, and in all three the damage is present in the model's own FIRST JSON delta, before any harness code runs. bin/dsh_session_audit.mjs re-attributes any future occurrence automatically.
23. [OPEN, harness, 2026-09-11] `image_block_guard.py` keys on the BACKEND HOST, so a DeepSeek session is denied a Read of any image even on the `deepseek-flash` (V4.1-Flash) tiers, which can now see. Verified live 2026-09-11: `deepseek-flash` read a 1x1 red PNG on the Anthropic-format route and answered "Red", while `_backend_capability.PROVEN_BLIND` still contains `deepseek` and `images_native` stays False. The deny is CORRECT today, not stale: a host-keyed guard cannot distinguish the `deepseek-flash` tiers from the `deepseek-v4-pro` opus rung, and the opus rung really is text-only, so letting the Read through would let a Pro session carry an image block it cannot read. Fix: make the guard resolve the session's MODEL id rather than only the base URL, then exempt `deepseek-flash` (and its `[1m]` form) while keeping `deepseek-v4-pro` denied. Touching this changes a machine-wide enforcement layer with its own test suite, which is why it was documented rather than changed in passing.

24. [RESOLVED 2026-09-11, in 0.1.5-rc.2] The fork-local `vision-routing` plugin defaulted to the retired `deepseek-v4-flash-vision-exp`. The installed profile's `dsh-vision-routing-0.1.5-rc.1` package declares `DEFAULT_ROUTE = { provider: "deepseek-official", model: "deepseek-v4-flash-vision-exp" }`. Impact is nil today: the default session model is `deepseek-flash`, which declares image input itself, so the router is only reached from a text-only session model (`deepseek-v4-pro`), and the retired id still answers at Flash price. FIXED IN 0.1.5-rc.2 (branch update/v0.1.5-rc.2, commit 7fdd0d6c9c): `DEFAULT_ROUTE` is now `deepseek-flash`, both READMEs and the package tests follow, and the packaged seed archive was read back carrying 1 occurrence of `deepseek-flash` and 0 of the retired id. LIVE as of the 2026-09-11 install: the RUNNING profile's `dsh-vision-routing/lib/index.js` carries `deepseek-flash` once and the retired id zero times.

25. [OPEN, harness, found 2026-09-11] `state_file_cap.py --repo` is DESTRUCTIVE on this repo and must not be run against it until fixed. After `CURRENT_STATE.md` was restored from `origin/master` at 42 sections, the tool reported a trim to 27522 bytes and, in doing so, EVICTED 26 of the 42 sections and reordered the survivors so that the NEWEST section (2026-09-11) ended up at line 286 instead of the top, with a stale `## Last save-state` block left near the head. The file is a newest-first log, so both the eviction volume and the reordering work against its contract; the damaged result was discarded and the file restored from `origin/master` a second time, and the tool was deliberately not re-run. The consequence is that `CURRENT_STATE.md` now sits at about 60 KB against a 32 KB ceiling. This is the same class as item 16b (`state_file_reconcile.py` reorders a table-ledger file when it recovers nothing): the shared eviction/union code is not order-stable. Fix: make both tools prove they are a no-op before writing, keep `## <date>` sections in descending date order, and never drop a section that is newer than the ones it keeps. Evidence: `origin/master` copy at 42 sections versus 16 after the run, and the restored file's section order.

26. [OPEN, 2026-09-11, DURING THE rc.2 SESSION] The destruction of item 16 happened a FOURTH time, and this time the timeline falls INSIDE a single session. At 09:42 local, `CURRENT_STATE.md` in the PRIMARY checkout held 30 dated sections against 33 in HEAD, and the newest section, `2026-09-11 - V4.1-Flash model ids`, was GONE; `git diff --numstat` reported 4 insertions and 128 deletions against HEAD. The save-state hook had written at 13:23:50Z under session `1e4d8e94-8f5a-4ced-a6bb-7e447b8fba33`, which is NOT the session that found the damage. Recovered with `git checkout HEAD -- CURRENT_STATE.md` (784 lines, 33 sections); the damaged copy is archived at `%TEMP%\deepseek-harness-CURRENT_STATE.damaged-2026-09-11-0942.md`. `VERIFICATION_RESULTS.md` was checked the same way and is INTACT at 28 sections, equal to HEAD. What this adds to item 16 is the timing: the writer is a CURRENT session's save-state run, not a stale worktree publishing late, which narrows the publisher question to the save-state path itself. Do not run `state_file_cap.py --repo` against this repo (item 25).

27. [OPEN, harness, 2026-09-11, NOT root-caused, nothing shipped] The All Sessions rail defect. The 2026-09-10 verification recorded that the `sidebar.allSessions` section disappears after collapsing and re-expanding the 56px sidebar rail and returns only after an app restart. TWO CANDIDATE CAUSES WERE ELIMINATED by new passing regression tests written in the rc.2 session: (a) the shell's `wide` flag does NOT latch across collapse, settle and expand (`packages/client/ui-sidebar/tests/sidebar-root.client.spec.tsx`), and (b) the slot registry DOES re-register the section after BOTH its declaring child slot and its declaring parent entry collapse and return (`packages/client/ui-workspace/tests/apply.client.spec.ts`). A read of the outlet dispatch (`packages/client/ui-renderer/src/client/scoped-slots.tsx`, lines 801-805 for single dispatch and 484-505 for entry props) shows owner props reach the entry component on every render, so there is no props-blind memoization. NO FIX WAS SHIPPED: a speculative change in slot-lifecycle code would be worse than the defect, and the plan's rule was a red-then-green test or nothing. The surviving candidate is the persisted fold store `dsh.workspace.allSessions.v1` (`packages/client/ui-workspace/src/client/stores.ts`, `createAllSessionsStore`), whose `expanded` default is `true`: a lost or stale persisted write would hide the list while a restart restores the default, which fits the reported symptom. NEXT: reproduce it in the REAL client (the built web client driven headlessly, else the packaged app) before changing anything, then write the failing test first.
